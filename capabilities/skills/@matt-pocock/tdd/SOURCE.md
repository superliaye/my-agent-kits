# Source

Vendored from <https://github.com/mattpocock/skills>, path `skills/engineering/tdd/`, reviewed at upstream HEAD `068b6e0c62393147daf03530149cdce209c93da8`.

The kit retains its self-contained TDD workflow and bundled references because current upstream delegates architecture vocabulary to `codebase-design`, which this kit does not ship. The stable `tdd` invocable name and red-green-refactor behavior are preserved.
